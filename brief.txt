<task>
Add the memorized-PIN second factor to the Flutter web admin app (`apps/admin/`). Admin login becomes THREE steps, every session: phone → WhatsApp OTP (already works) → memorized PIN (new) → dashboard. Plus a first-run PIN setup screen for admins who have not set a PIN yet. Backend RPCs are planned but NOT yet deployed (`verify_admin_pin(p_pin) -> boolean`, `set_admin_pin(p_pin)`, `admin_pin_status() -> 'SET'|'UNSET'|'LOCKED'`) — the app must call them by those exact names via `SupabaseClient.rpc`, and tests mock them; do not create or edit any migration.

READ FIRST (authoritative spec): `docs/superpowers/plans/2026-08-12-auth-rework-phone-first.md` — §3 (model table) and §6 (admin web work item). Also read `docs/superpowers/plans/conventions.md` for app conventions.

CURRENT STATE (read before editing):
- `apps/admin/lib/core/auth/admin_auth_provider.dart` — `AdminAuthNotifier` (NotifierProvider `adminAuthProvider`). States: `AdminAuthStatus { initial, loading, authenticated, accessDenied, unauthenticated }`. `sendOtp(phone)` → Supabase `signInWithOtp`; `verifyOtp(phone, token)` → `verifyOTP` then `_verifyRole(user)` → queries `users.role` (and tries `is_admin_or_priest` rpc first) → `authenticated` when role == 'ADMIN', else `accessDenied`. Arabic error messages already in place.
- `apps/admin/lib/features/auth/admin_login_screen.dart` — RTL ConsumerStatefulWidget, 2 steps via `_otpSent` flag (phone field → OTP field), shows Dashboard stub when `authenticated`.
- Tests: `apps/admin/test/features/auth/admin_auth_test.dart` — `createMockSupabaseClient(role)` MockClient handling `/auth/v1/otp`, `/auth/v1/verify`, `/rest/v1/users`; provider + login-screen widget tests. `apps/admin/test/widget/router_test.dart` + `widget_test.dart` cover routing/full app (a mock Supabase client builder is shared somewhere under `apps/admin/test/` — find and reuse it).

WHAT TO CHANGE:
1. `admin_auth_provider.dart`:
   - Add statuses to `AdminAuthStatus`: `pinRequired` (OTP+role passed, PIN must be entered), `pinSetupRequired` (first run: OTP+role passed but no PIN set yet), and keep `authenticated` only reached after successful PIN (or after PIN setup completes).
   - After `_verifyRole` succeeds (role == ADMIN), call RPC `admin_pin_status()`. Map result: 'SET' → `pinRequired`; 'UNSET' → `pinSetupRequired`; 'LOCKED' → `accessDenied` with Arabic lockout message; error/exception → keep `unauthenticated` with Arabic message. (Catch rpc errors — the RPC may be missing in local/test environments; if it errors or returns non-string, fall back to `pinRequired` so the PIN form shows.)
   - New methods: `verifyPin(String pin)` → `rpc('verify_admin_pin', {p_pin: pin})` → boolean true → `authenticated`; false → `unauthenticated` + Arabic wrong-PIN message. `setPin(String pin)` → `rpc('set_admin_pin', {p_pin: pin})` → success → `authenticated`; failure → `unauthenticated` + Arabic error. PIN validation 4–6 digits enforced in the UI layer.
   - Add Arabic strings (PIN prompt, wrong PIN, set-PIN title, PIN mismatch, lockout) as themed `Text` in the screens — follow the existing inline Arabic string style in `admin_login_screen.dart`.
2. `admin_login_screen.dart`: three steps driven by authState status — phone step, OTP step, then when `pinRequired`: PIN input (4–6 digits, obscured, numeric) + confirm field on `pinSetupRequired` (enter + confirm). Reuse the existing card layout/buttons. `authenticated` renders the existing dashboard stub. Keep `_otpSent` flag or replace with status-driven rendering — your call, keep behavior testable.
3. Tests: extend `admin_auth_test.dart` (and router/widget tests if they break) —
   - Mock `/rest/v1/rpc/admin_pin_status` (return e.g. `"SET"`), `/rest/v1/rpc/verify_admin_pin` (true/false), `/rest/v1/rpc/set_admin_pin` (empty/ok) in `createMockSupabaseClient` (add a param e.g. `pinStatus`, `pinValid`).
   - Provider tests: OTP verify + pinStatus SET → `pinRequired`; verifyPin(true) → `authenticated`; verifyPin(false) → error; pinStatus UNSET → `pinSetupRequired`; setPin success → `authenticated`.
   - Widget tests: full 3-step login flow renders each step; wrong PIN shows error; setup flow shows confirm field; router test: app lands on login when logged out (should already exist — keep green).

DON'T: touch migrations/RPCs/SQL, the mobile app, the OTP flow itself, or change `_verifyRole` semantics for the role check. `sendOtp`/`verifyOtp` behavior stays identical.
</task>

<verification_loop>
Run and make green before finishing (fix, don't just report):
  - `flutter test test`   (run from apps/admin; ALL tests must pass, including pre-existing ones)
  - `flutter analyze lib test`
Confirm `git status` afterwards shows ONLY intended changes under `apps/admin/`.
</verification_loop>

<action_safety>
Scope strictly to `apps/admin/` (auth provider + login screen + their tests). No unrelated refactors, no theme changes, no dependency/version changes, no edits to analytics/dashboard screens. Do NOT run git add or git commit — the orchestrator commits after review. Leave work uncommitted.
</action_safety>

<structured_output_contract>
End with a report in EXACTLY this shape:
  1. What changed and why (the 3-step flow + setup flow in a few lines)
  2. Files touched (grouped: edited / new / deleted)
  3. Gate outcomes (paste flutter test summary counts and flutter analyze result)
  4. Anything you deviated on, left open, or want a decision on
</structured_output_contract>