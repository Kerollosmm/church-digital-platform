/**
 * ============================================================================
 * Church Digital Platform â€” Predefined Seed Test Accounts & Switcher UI
 * ============================================================================
 */

(function () {
  'use strict';

  
  // ---------------------------------------------------------------------------
  // Per-run credentials (FR-006): passwords are generated by run.ps1 into
  // shared/.local-credentials.json (gitignored) — never committed.
  // ---------------------------------------------------------------------------
  let passwordsPromise = null;
  function loadPasswords() {
    if (!passwordsPromise) {
      passwordsPromise = fetch('/shared/.local-credentials.json')
        .then(function (r) { return r.ok ? r.json() : {}; })
        .catch(function () { return {}; });
    }
    return passwordsPromise;
  }

  async function getPasswordFor(account) {
    const map = await loadPasswords();
    return map[account.phone] || '';
  }


    {
      id: 'mariam',
      name: 'Ù…Ø±ÙŠÙ… ÙŠÙˆØ³Ù',
      email: 'mariam@church.test',
      phone: '+201000000002',
      password: null, // loaded per-run from untracked .local-credentials.json
      otp: '123456',
      role: 'USER',
      roleLabel: 'Ù…Ø®Ø¯ÙˆÙ…Ø© / Ø±Ø¹ÙŠØ©',
      badgeClass: 'badge-role-user',
      avatar: 'Ù…',
      description: 'Ø­Ø³Ø§Ø¨ Ù…Ø³ØªØ®Ø¯Ù… Ù„Ø§Ø®ØªØ¨Ø§Ø± Ø­Ø¬Ø² Ø§Ù„Ù…ÙˆØ§Ø¹ÙŠØ¯ ÙˆØ§Ù„Ù‚Ø¯Ø§Ø³Ø§Øª ÙˆØ¯ÙØ¹ Paymob ÙˆØ§Ù„Ø´ÙƒØ§ÙˆÙ‰'
    },
    {
      id: 'peter',
      name: 'Ø¨ÙŠØªØ± Ø³Ù…ÙŠØ±',
      email: 'peter@church.test',
      phone: '+201000000003',
      password: null, // loaded per-run from untracked .local-credentials.json
      otp: '123456',
      role: 'USER',
      roleLabel: 'Ù…Ø®Ø¯ÙˆÙ… / Ø±Ø¹ÙŠØ©',
      badgeClass: 'badge-role-user',
      avatar: 'Ø¨',
      description: 'Ø­Ø³Ø§Ø¨ Ù…Ø³ØªØ®Ø¯Ù… Ø«Ø§Ù†Ù Ù„Ø§Ø®ØªØ¨Ø§Ø± Ø§Ù„ØªÙ†Ø§ÙØ³ Ø¹Ù„Ù‰ Ø§Ù„Ù…Ù‚Ø§Ø¹Ø¯ ÙˆØ§Ù„Ø­Ø¬Ø² Ø§Ù„Ù…ØªØ²Ø§Ù…Ù†'
    },
    {
      id: 'admin',
      name: 'Ø£/ Ù…ÙŠÙ†Ø§ Ù…ÙˆØ±ÙŠØ³',
      email: 'admin@church.test',
      phone: '+201000000001',
      password: null, // loaded per-run from untracked .local-credentials.json
      otp: '123456',
      role: 'ADMIN',
      roleLabel: 'Ø³ÙƒØ±ØªÙŠØ± Ø¥Ø¯Ø§Ø±ÙŠ',
      badgeClass: 'badge-role-admin',
      avatar: 'Ù…',
      description: 'Ø­Ø³Ø§Ø¨ Ø¥Ø¯Ø§Ø±ÙŠ Ù„Ø§Ø®ØªØ¨Ø§Ø± ØªØ£ÙƒÙŠØ¯ Ø§Ù„Ø­Ø¬ÙˆØ²Ø§Øª Ø¨Ø¹Ø¯ Ø§Ù„Ø§ØªØµØ§Ù„ ÙˆØ¥Ø¯Ø§Ø±Ø© Ø§Ù„Ø³Ø¹Ø© ÙˆØ§Ù„Ø­Ø¬Ø² Ø§Ù„Ù†Ù‚Ø¯ÙŠ'
    },
    {
      id: 'priest',
      name: 'Ø£Ø¨ÙˆÙ†Ø§ Ø¨ÙˆÙ„Ø§',
      email: 'priest@church.test',
      phone: '+201000000004',
      password: null, // loaded per-run from untracked .local-credentials.json
      otp: '123456',
      role: 'ADMIN',
      roleLabel: 'ÙƒØ§Ù‡Ù† Ø§Ù„ÙƒÙ†ÙŠØ³Ø©',
      badgeClass: 'badge-role-admin',
      avatar: 'âœ',
      description: 'Ø­Ø³Ø§Ø¨ ÙƒØ§Ù‡Ù† Ù„Ù…ØªØ§Ø¨Ø¹Ø© Ø§Ù„Ù…ÙˆØ§Ø¹ÙŠØ¯ ÙˆÙÙƒ ØªØ´ÙÙŠØ± ÙˆÙ‚Ø±Ø§Ø¡Ø© Ø´ÙƒØ§ÙˆÙ‰ Ø§Ù„Ù…Ø®Ø¯ÙˆÙ…ÙŠÙ†'
    },
    {
      id: 'superadmin',
      name: 'Ù…/ ÙƒÙŠØ±Ù„Ø³ - Ù…Ø¯ÙŠØ± Ø§Ù„Ù†Ø¸Ø§Ù…',
      email: 'superadmin@church.local',
      phone: '+201000000005',
      password: null, // loaded per-run from untracked .local-credentials.json
      otp: '123456',
      role: 'SUPER_ADMIN',
      roleLabel: 'Ù…Ø¯ÙŠØ± Ø§Ù„Ù…Ù†Ø¸ÙˆÙ…Ø© Ø§Ù„Ø£Ø¹Ù„Ù‰',
      badgeClass: 'badge-role-superadmin',
      avatar: 'âš¡',
      description: 'Ø­Ø³Ø§Ø¨ Ø¥Ø¯Ø§Ø±Ø© Ø¹Ù„ÙŠØ§ Ù„Ø¶Ø¨Ø· Ø§Ù„ØµÙ„Ø§Ø­ÙŠØ§Øª ÙˆÙ…Ø·Ø§Ù„Ø¹Ø© Ø³Ø¬Ù„ Ø§Ù„ØªØ¯Ù‚ÙŠÙ‚ ÙˆØ§Ù„ØªØ­Ù„ÙŠÙ„Ø§Øª Ø§Ù„Ù…Ø§Ù„ÙŠØ©'
    }
  ];

  /**
   * Log in directly with predefined account
   */
  async function loginAsAccount(accountIdOrEmail) {
    const acc = TEST_ACCOUNTS.find(
      (a) => a.id === accountIdOrEmail || a.email.toLowerCase() === accountIdOrEmail.toLowerCase()
    );

    if (!acc) {
      throw new Error(`Ø­Ø³Ø§Ø¨ ØºÙŠØ± Ù…Ø¹Ø±ÙˆÙ: ${accountIdOrEmail}`);
    }

    const sb = (typeof window !== 'undefined' && window.ChurchSupabase) ? window.ChurchSupabase : ((typeof globalThis !== 'undefined' && globalThis.ChurchSupabase) ? globalThis.ChurchSupabase : null);
    if (!sb || !sb.auth) {
      throw new Error('ChurchSupabase client is not loaded.');
    }

    try {
      const pass = await getPasswordFor(acc);
      const data = await sb.auth.loginWithPassword(acc.email, pass);
      sb.showToast(`ØªÙ… ØªØ³Ø¬ÙŠÙ„ Ø§Ù„Ø¯Ø®ÙˆÙ„ Ø¨Ù†Ø¬Ø§Ø­ ÙƒÙ€: ${acc.name} (${acc.roleLabel})`, 'success');
      return { success: true, account: acc, data };
    } catch (err) {
      const msg = err.message || 'ÙØ´Ù„ ØªØ³Ø¬ÙŠÙ„ Ø§Ù„Ø¯Ø®ÙˆÙ„';
      sb.showToast(msg, 'error');
      throw err;
    }
  }

  /**
   * Render interactive test account picker inside container
   */
  function renderAccountGrid(containerIdOrElement, options = {}) {
    const container = typeof containerIdOrElement === 'string'
      ? document.getElementById(containerIdOrElement)
      : containerIdOrElement;

    if (!container) return;

    const onLoginSuccess = options.onSuccess || (() => window.location.reload());
    const filterRole = options.role || null;

    const accountsToShow = filterRole
      ? TEST_ACCOUNTS.filter(a => a.role === filterRole || (filterRole === 'ADMIN' && a.role === 'SUPER_ADMIN'))
      : TEST_ACCOUNTS;

    let html = `
      <div class="test-accounts-wrapper" style="display:flex; flex-direction:column; gap:12px;">
        <div style="font-size:0.85rem; color:var(--text-muted); margin-bottom:4px;">
          âš¡ Ø§Ø¶ØºØ· Ø¹Ù„Ù‰ Ø£ÙŠ Ø­Ø³Ø§Ø¨ Ù„ØªØ³Ø¬ÙŠÙ„ Ø§Ù„Ø¯Ø®ÙˆÙ„ Ø§Ù„ÙÙˆØ±ÙŠ Ø¨ÙƒÙ„Ù…Ø© Ù…Ø±ÙˆØ± <code style="color:var(--gold);">كلمة المرور المولّدة لهذه الجلسة</code> Ùˆ OTP <code style="color:var(--gold);">123456</code>:
        </div>
        <div style="display:grid; grid-template-columns: repeat(auto-fit, minmax(260px, 1fr)); gap:10px;">
    `;

    accountsToShow.forEach((acc) => {
      html += `
        <div class="card card-interactive account-btn" data-account-id="${acc.id}" style="padding:12px 14px; cursor:pointer; display:flex; align-items:center; justify-content:space-between; gap:10px;">
          <div style="display:flex; align-items:center; gap:10px;">
            <div class="user-avatar" style="width:34px; height:34px; font-size:0.95rem; font-weight:bold;">${acc.avatar}</div>
            <div>
              <div style="font-weight:700; font-size:0.9rem; color:var(--text-primary);">${acc.name}</div>
              <div style="font-size:0.75rem; color:var(--text-secondary); direction:ltr; text-align:right;">${acc.email}</div>
            </div>
          </div>
          <div style="display:flex; flex-direction:column; align-items:flex-end; gap:4px;">
            <span class="badge ${acc.badgeClass}">${acc.roleLabel}</span>
            <span style="font-size:0.7rem; color:var(--text-muted); direction:ltr;">${acc.phone}</span>
          </div>
        </div>
      `;
    });

    html += `
        </div>
      </div>
    `;

    container.innerHTML = html;

    // Attach click listeners
    const buttons = container.querySelectorAll('.account-btn');
    buttons.forEach((btn) => {
      btn.addEventListener('click', async () => {
        const accountId = btn.getAttribute('data-account-id');
        btn.style.opacity = '0.6';
        btn.style.pointerEvents = 'none';
        try {
          const res = await loginAsAccount(accountId);
          if (onLoginSuccess) {
            onLoginSuccess(res);
          }
        } catch (e) {
          // Toast handled in loginAsAccount
        } finally {
          btn.style.opacity = '1';
          btn.style.pointerEvents = 'auto';
        }
      });
    });
  }

  /**
   * Render top user banner / status pill
   */
  async function renderTopUserBanner(containerIdOrElement, options = {}) {
    const container = typeof containerIdOrElement === 'string'
      ? document.getElementById(containerIdOrElement)
      : containerIdOrElement;

    if (!container) return;

    const sb = (typeof window !== 'undefined' && window.ChurchSupabase) ? window.ChurchSupabase : ((typeof globalThis !== 'undefined' && globalThis.ChurchSupabase) ? globalThis.ChurchSupabase : null);
    if (!sb) return;

    const user = await sb.auth.getUser();
    if (!user) {
      container.innerHTML = `
        <div style="display:flex; align-items:center; gap:8px;">
          <span class="badge badge-closed">ØºÙŠØ± Ù…Ø³Ø¬Ù„ Ø§Ù„Ø¯Ø®ÙˆÙ„</span>
          <a href="/index.html" class="btn btn-sm btn-outline">ØªØ³Ø¬ÙŠÙ„ Ø§Ù„Ø¯Ø®ÙˆÙ„</a>
        </div>
      `;
      return;
    }

    const role = await sb.auth.getCurrentUserRole();
    const matchedAccount = TEST_ACCOUNTS.find(a => a.email.toLowerCase() === (user.email || '').toLowerCase());
    const displayName = matchedAccount ? matchedAccount.name : (user.user_metadata?.name || user.email || 'Ù…Ø³ØªØ®Ø¯Ù…');
    const badgeClass = role === 'SUPER_ADMIN' ? 'badge-role-superadmin' : (role === 'ADMIN' ? 'badge-role-admin' : 'badge-role-user');
    const roleLabel = role === 'SUPER_ADMIN' ? 'Ù…Ø¯ÙŠØ± Ø£Ø¹Ù„Ù‰' : (role === 'ADMIN' ? 'Ø¥Ø¯Ø§Ø±ÙŠ' : 'Ù…Ø®Ø¯ÙˆÙ…');

    container.innerHTML = `
      <div style="display:flex; align-items:center; gap:10px;">
        <div class="user-pill">
          <div class="user-avatar">${matchedAccount ? matchedAccount.avatar : 'ðŸ‘¤'}</div>
          <span>${displayName}</span>
          <span class="badge ${badgeClass}">${roleLabel}</span>
        </div>
        <button id="btn-logout" class="btn btn-sm btn-outline" style="padding:4px 8px; font-size:0.75rem;">Ø®Ø±ÙˆØ¬</button>
      </div>
    `;

    const logoutBtn = container.querySelector('#btn-logout');
    if (logoutBtn) {
      logoutBtn.addEventListener('click', async () => {
        await sb.auth.logout();
        sb.showToast('ØªÙ… ØªØ³Ø¬ÙŠÙ„ Ø§Ù„Ø®Ø±ÙˆØ¬ Ø¨Ù†Ø¬Ø§Ø­', 'info');
        setTimeout(() => {
          if (options.onLogout) {
            options.onLogout();
          } else {
            window.location.reload();
          }
        }, 400);
      });
    }
  }

  const TestAccounts = {
    ACCOUNTS: TEST_ACCOUNTS,
    loginAs: loginAsAccount,
    renderGrid: renderAccountGrid,
    renderUserBanner: renderTopUserBanner
  };

  if (typeof window !== 'undefined') {
    window.TestAccounts = TestAccounts;
  }
  if (typeof globalThis !== 'undefined') {
    globalThis.TestAccounts = TestAccounts;
  }
})();

